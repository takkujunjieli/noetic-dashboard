name: Retail flow 引擎
on:
  workflow_dispatch:
    inputs:
      sym:
        description: "标的(逗号分隔;留空=research 页选取 config/retail_syms.json。~2-15min/票)"
        required: false
        default: ""
      day:
        description: "单个目标日 YYYY-MM-DD(留空=最近收盘工作日)"
        required: false
        default: ""
      backfill:
        description: "回填最近 N 个工作日(0=不回填;断点续跑,可重复触发直到跑满)"
        required: false
        default: "0"
      days:
        description: "显式日期列表 逗号分隔(覆盖 backfill/day)"
        required: false
        default: ""
      force:
        description: "1 = 强制重跑(覆盖已采集的 day,票;用于测试/纠正)"
        required: false
        default: "0"
  schedule:
    - cron: "0 3 * * 2-6"        # 夜间空闲:03:00 UTC(美股收盘后数小时、网关空闲)跑上一交易日;周二~周六覆盖周一~周五盘

concurrency:
  group: retailflow-pit
  cancel-in-progress: false

permissions:
  contents: write

jobs:
  flow:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with: { python-version: "3.12" }
      - run: pip install requests "urllib3<2" pytrends "exchange-calendars>=4.5,<5"   # urllib3<2:修 pytrends method_whitelist

      - name: Verify PIT invariants
        run: python -m unittest discover -s tests -p "test_retail_pit.py"

      - name: 散户流·采样引擎(分钟量选窗 + 分层比率估计 + 每日一票全量验证)
        timeout-minutes: 300       # 5h 上限;超时被杀后仍走 always() 提交已完成的天
        run: python -u scripts/fetch_tick_flow.py   # -u:不缓冲,超时被杀也能看到已完成进度
        env:
          MASSIVE_API_KEY: ${{ secrets.MASSIVE_API_KEY }}
          MASSIVE_BASE_URL: ${{ vars.MASSIVE_BASE_URL }}
          RF_SAMPLE: "1"                  # 采样模式:只下高价值窗口(~50min/票),支持全组 28+ 只/夜
          RF_SYMS: ${{ inputs.sym }}      # 空=config/retail_syms.json(research 页多选下拉编辑,独立于 D/Q)
          RF_DAY: ${{ inputs.day }}
          RF_BACKFILL: ${{ inputs.backfill || '0' }}
          RF_DAYS: ${{ inputs.days }}
          RF_FORCE: ${{ inputs.force || '0' }}

      - name: Attention(Google Trends, best-effort)
        if: always()
        run: python scripts/fetch_trends.py || true
        env:
          RF_SYMS: ${{ inputs.sym }}   # 空=fetch_trends 读 config/retail_syms.json,与引擎标的一致

      - name: 冻结实际可用时间的 PIT 信号
        if: always()
        run: python scripts/build_retailflow.py

      - name: 获取冻结股票池的独立前瞻价格
        if: always()
        run: python scripts/fetch_retail_prices.py
        env:
          MASSIVE_API_KEY: ${{ secrets.MASSIVE_API_KEY }}
          MASSIVE_BASE_URL: ${{ vars.MASSIVE_BASE_URL }}

      - name: 组装 retailflow.json
        if: always()
        run: python scripts/build_retailflow.py

      - name: 提交数据到 main
        if: always()
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add data/retail_flow_raw.json data/retail_trends.json data/retailflow.json data/retail_pit.json data/retail_prices.json 2>/dev/null || true
          if ! git diff --cached --quiet; then
            git commit -m "chore: retailflow $(date -u +%F\ %R)"
            git pull --rebase -X theirs origin main
            git push || { git pull --rebase -X theirs origin main && git push; }
          else
            echo "无数据变化"
          fi

  deploy:
    needs: flow
    if: always()
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pages: write
      id-token: write
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - uses: actions/checkout@v4
        with: { ref: main }
      - run: bash scripts/cache_bust.sh
      - uses: actions/configure-pages@v5
      - uses: actions/upload-pages-artifact@v3
        with:
          path: .
      - id: deployment
        uses: actions/deploy-pages@v4
